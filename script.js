const screens=[...document.querySelectorAll(".screen")];const nav=[...document.querySelectorAll(".nav-item")];const splash=document.getElementById("splash");const backBtn=document.getElementById("backBtn");let current="home";let historyStack=[];function showScreen(id,push=true){const target=document.getElementById(id);if(!target)return;if(push&&current!==id)historyStack.push(current);screens.forEach(s=>s.classList.toggle("active",s.id===id));nav.forEach(n=>n.classList.toggle("active",n.dataset.go===id));current=id;window.scrollTo({top:0,behavior:"smooth"});document.title=`${id.charAt(0).toUpperCase()+id.slice(1)} | BR7 Gym`;}document.querySelectorAll("[data-go]").forEach(el=>el.addEventListener("click",()=>showScreen(el.dataset.go)));backBtn.addEventListener("click",()=>{if(historyStack.length)showScreen(historyStack.pop(),false);else showScreen("home",false)});document.getElementById("profileBtn").addEventListener("click",()=>showScreen("profile"));document.getElementById("notifyBtn").addEventListener("click",()=>showToast("No new notifications"));function selectPlan(plan){showToast(`${plan} plan selected — connect BR7's payment system to continue.`)}function showToast(message){const t=document.getElementById("toast");t.textContent=message;t.classList.add("show");clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>t.classList.remove("show"),2800)}setTimeout(()=>splash.classList.add("hide"),1200);